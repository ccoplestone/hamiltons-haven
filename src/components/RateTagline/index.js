import React from 'react';
import PropTypes from 'prop-types';

import styles from './RateTagline.module.scss';

const RateTagline = () => <div className={styles.root}>No Cash, No Splash</div>;

RateTagline.propTypes = {};

RateTagline.defaultProps = {};

export default RateTagline;
